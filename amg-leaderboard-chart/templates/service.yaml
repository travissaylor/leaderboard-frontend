apiVersion: v1
kind: Service
metadata:
  name: {{ include "amg-leaderboard-chart.fullname" . }}
  labels:
{{ include "amg-leaderboard-chart.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "amg-leaderboard-chart.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
